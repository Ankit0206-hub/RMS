import React from 'react';

const MenuVariants = () => {
    return (
        <div className="bg-white p-8 rounded-xl shadow-sm border border-gray-100 flex items-center justify-center h-[500px]">
            <div className="text-center">
                <h2 className="text-2xl font-bold text-gray-900 mb-2">Menu Variants</h2>
                <p className="text-gray-500">Manage item variants (e.g., Small, Medium, Large).</p>
            </div>
        </div>
    );
};

export default MenuVariants;
